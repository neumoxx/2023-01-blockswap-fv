diff --git a/certora/munged/syndicate/Syndicate.sol b/certora/munged/syndicate/Syndicate.sol
index 07f69c8..50cb044 100644
--- a/certora/munged/syndicate/Syndicate.sol
+++ b/certora/munged/syndicate/Syndicate.sol
@@ -679,7 +679,7 @@ contract Syndicate is ISyndicateInit, Initializable, Ownable, ReentrancyGuard, S
     function _claimAsStaker(address _recipient, blsKey[] memory _blsPubKeys) internal {
         uint256 numOfKeys = _blsPubKeys.length;
         if (numOfKeys == 0) revert EmptyArray();
-        if (_recipient == address(0)) revert ZeroAddress();
+        //if (_recipient == address(0)) revert ZeroAddress();
         if (_recipient == address(this)) revert ZeroAddress();
 
         // Make sure we have the latest accrued information
